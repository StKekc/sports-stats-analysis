# sports-stats-analysis
Проект анализа данных с сайта sports-reference.com. Включает сбор и очистку данных, загрузку в PostgreSQL, построение витрин и исследовательский анализ (кластеризация, динамика формы, личные встречи и др.).

## 📌 Описание проекта
Мы формируем единую базу футбольной статистики на основе выгрузок с сайта FBref.com и агрегируем данные по многим лигам и сезонам.  
Поверх этой базы строятся представления и аналитические ноутбуки для ответа на типовые вопросы футбольной аналитики: стиль команды, форма по месяцам, влияние фактора поля, «неудобные» соперники и т.д.


## 🎯 Задачи
1. Кластеризация команд по стилю игры на основе различных метрик (владение мячом, количество ударов, количество фолов, количество передач и др.).
2. Подсчет процента побед в зависимости от фактора игры (домашняя или выездная).
3. Построение динамики результатов команды по сезонам и месяцам — выявление пиков и спадов формы.
4. Определение «самого неудобного соперника» для каждой команды.
5. Прогноз исхода матча на основе данных о форме команды и статистики соперников.

## 📊 Источники данных и объём

- Источник: выгрузки CSV с сайта FBref.com (разделы по топ-лигам и другим турнирам).
- Структура сырых данных: `data/raw/fbref/…`.
- Объём после миграции в БД:  
  ~2.5 ГБ данных, 15 лиг × 6 сезонов, ≈400 команд, ≈10 000 игроков, ≈30 000 матчей.

Мигрируются:

- Справочники: лиги, сезоны, команды, игроки.
- Матчи: расписание, счёт, xG и базовая статистика.
- Турнирные таблицы.
- Командная статистика по сезонам.
- Игроковая статистика девяти типов (standard, shooting, passing, defense, possession, misc, passing_types, keepers, keepers_adv).

## 🗂 Структура репозитория

- `data/`
  - `raw/` — сырые CSV с FBref.
  - `processed/` — предобработанные данные и промежуточные выгрузки.
- `database/`
  - `schema.sql` — SQL-схема PostgreSQL.
  - `config.yaml` — конфигурация подключения и путей ETL.
  - `etl_utils.py`, `etl_loaders.py`, `player_stats_loader.py` — утилиты и загрузчики данных.
  - `migrate_to_postgres.py` — основной скрипт миграции (полный ETL-пайплайн).
  - `README.md`, `QUICKSTART.md` — подробная документация по БД и миграции.
- `src/`
  - Скрипты парсинга/скачивания данных с FBref.
  - Вспомогательные модули для анализа, визуализации и подготовки признаков.
- `notebooks/`
  - Jupyter-ноутбуки с исследовательским анализом и примерами использования БД.
- `requirements.txt`
  - Список основных Python-зависимостей.
- `LICENSE`
  - Лицензия MIT.

## 🧩 Стек технологий

**Языки и инструменты**

- Python 3.8+  
- PostgreSQL 12+

**Python-библиотеки**

- Работа с данными: `pandas`, `numpy`, `python-dateutil`.
- Веб-доступ и парсинг: `requests`, `beautifulsoup4`, `lxml`, `playwright`, `tqdm`.
- Базы данных: `psycopg2-binary`, `pyyaml`.
- Обработка больших данных: `pyspark`, `py4j`.
- Визуализация: `matplotlib`, `seaborn`, `plotly`, `kaleido`.

